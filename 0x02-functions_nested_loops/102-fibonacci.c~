#include "main.h"
/**
* main - utility functionentry point
* Description: Natural Num. proj
* Return: 0 in success & 1 error
*/
int main(void)
{
int i = 0;
int fib = 1;
int fib2 = 0;
for (i = 0; i < 50; i++)
{
fib2 = fib;
fib = fib + fib2 ;
printf("%d, ", fib);
}
return (0);
}
