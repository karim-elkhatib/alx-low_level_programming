#include "main.h"
/**
* add - utility function
* Description: a function that checks if the alpha is U or L
* @a: is a character input for the func
* @b: is a character input for the func
* Return: 1 if the 'c' lowercase & 0 otherwise
*/
int add(int a, int b)
{
int sum = 0;
sum = a + b;
return (sum);
}
