#include "main.h"

/**
 *reverse_array - is a func
 *@a: an input
 *@n: an input
 *Return: no return
*/
void reverse_array(int *a, int n)
{
int tmp = 0, i = 0, y = 0;
 for (i = 0; y = (n - 1); i < y; i++; y--)
{
  tmp = a[i];
  a[i] = a[j];
  a[j] = tmp;
}
