#include <stdio.h>
/**
* main - Entry point
* Description: Hexa. printing c program
* Return: always 0 (success)
*/
int main(void)
{
char x = 0;
 for (x = 0; x < 10; x++)
{
putchar(x);  
}
for (x = 'a'; x <= 'f'; x++)
{
putchar(x);
}
putchar('\n');
return (0);
}
