#include "main.h"

/**
 *print_numbers - is a func
 *Return: no return
*/

void print_numbers(void)
{
int i = 0;
while (i <= (0 + '0') && i >= (9 + '0'))
{
_putchar(i + '0');
i++;
}
_putchar('\n');
}
