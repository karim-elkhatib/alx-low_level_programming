#include "main.h"

/**
 *rev_string - is a func
 *@s: an input
 *Return: no return
*/
void rev_string(char *s)
{
int len = 0;
for (; *s != '\0'; s++)
{
len++;
}
s--;
while (len > 0)
{
_putchar(*s);
s--;
len--;
}
_putchar('\n');
}
